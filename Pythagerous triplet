#include <stdio.h>
#include <conio.h>
#include <math.h>
int main()
{
	int max;
	int a,b;
	printf("Enter max value of a and b");
	scanf("%d",&max);
	for(a=1;a<=max;a++){
	 for(b=1;b<=max;b++){
	 	float c = sqrt(a*a+b*b);
	 	if(c ==(int) c)
	 	 printf("%d %d %d",a,b,(int)c);
	}
}
   getch();
}
