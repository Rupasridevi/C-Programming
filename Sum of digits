#include<stdio.h>
int main()
{
	int num,sum=0,ori,d;
	printf("enter number :");
	scanf("%d",&num);
	if(num<=0)
      printf("INVALID");
	else
	{
	ori=num;
	while(num!=0)
	{
		d=num%10;
		sum=sum+d;
		num=num/10;
	}
	printf("sum of given number %d\t=%d",ori,sum);
}
return 0;
}
