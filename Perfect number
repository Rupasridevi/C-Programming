#include <stdio.h>
int main()
{
	int i,x,num,rem,sum;
	printf("enter a number\n");
	scanf("%d",& num);
	for(i=1;i<num;i++)
	 {
	 	rem = num% i;
	 	if(rem ==0)
	 	{
	 	  sum = sum+i;
		 }
	 }
	if(sum ==num)
	return 0;
	
}
